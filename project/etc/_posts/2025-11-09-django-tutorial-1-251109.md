---
layout: post
title: "[django] Django Tutorial - TEST CRUD API 1"
date: 2025-11-09 20:32:20 +0900
description: A lightweight Django project demonstrating basic CRUD operations and MVC structure.
img: # Add image post (optional)
fig-caption: # Add figcaption (optional)
categories: [project, etc]
---
# Django REST Framework를 사용하여 게시판 CRUD API
## 필요 패키지 설치
```bash
pip install djangorestframework
```
## `board/models.py`
### 1️⃣ 모델 기본 구조
```python
class Post(models.Model):
```
models.Model을 상속 → Django에게 이 클래스가 DB 테이블과 연결된다는 의미

Spring Boot의 @Entity와 비슷함

### 2️⃣ 필드 정의

| 필드           | Django 타입                             | 의미      | 옵션                                      |
| ------------ | ------------------------------------- | ------- | --------------------------------------- |
| `title`      | `CharField(max_length=200)`           | 글 제목    | `verbose_name='Title'` → admin 화면 컬럼 이름 |
| `content`    | `TextField()`                         | 글 내용    | admin 표시 이름 지정                          |
| `author`     | `CharField(max_length=100)`           | 작성자     | verbose_name 있음                         |
| `created_at` | `DateTimeField(default=timezone.now)` | 글 작성 시간 | 기본값 현재 시간                               |
| `updated_at` | `DateTimeField(default=timezone.now)` | 글 수정 시간 | 기본값 현재 시간                               |

💡 verbose_name → Django Admin이나 ModelForm에서 사람이 보기 좋은 이름으로 표시

💡 default=timezone.now → 객체 생성 시 자동으로 현재 시간 넣어줌

> Spring Boot에서는 이런 기능을 엔티티에 직접 지정하지 않고 Repository나 쿼리에서 처리하는 경우가 많음


### 3️⃣ Meta 클래스
```python
class Meta:
ordering = ['-created_at']
verbose_name = 'Post'
verbose_name_plural = 'Posts'
```

- `ordering = ['-created_at']` → 조회 시 기본 정렬: 최신글 순
- `verbose_name` → Admin에 표시할 단수 이름
- `verbose_name_plural` → Admin에 표시할 복수 이름

> Spring Boot에서는 이런 기능을 엔티티에 직접 지정하지 않고 Repository나 쿼리에서 처리하는 경우가 많음

### 4️⃣ 문자열 표현
```python
def __str__(self):
return self.title
```
- 객체를 문자열로 표현할 때 사용 → Admin 화면이나 shell에서
- Spring Boot에서는 toString() 오버라이드와 동일

### ✅ 요약
- `Post` 모델 = DB 테이블
- 필드 하나하나 = 컬럼
- `Meta` = 모델 전체 설정 (정렬, 이름 등)
- `__str__` = 객체 출력 시 문자열 표시

## `board/serializers.py`
### `serializers.py`
#### (1) `PostSerializer`
```python
from rest_framework import serializers
from .models import Post

class PostSerializer(serializers.ModelSerializer):
class Meta:
model = Post
fields = ['id', 'title', 'content', 'author', 'created_at', 'updated_at']
read_only_fields = ['id', 'created_at', 'updated_at']
```
- 역할: Django 모델(Post)과 JSON 데이터 변환을 담당
  - DB 객체 → JSON (Response)
  - JSON → DB 객체 (Request)

- `Meta`
  - `model = Post` → 어떤 모델과 연결될지 지정
  - `fields` → 직렬화할 필드 목록
  - `read_only_fields` → 읽기 전용 필드 (`id`, 생성/수정 시간 등 자동 처리되는 값)
> Spring Boot의 Jackson + DTO 느낌

### (2) `PostListSerializer`
```python
class PostListSerializer(serializers.ModelSerializer):
"""목록 조회용 - 간단한 정보만"""
class Meta:
model = Post
fields = ['id', 'title', 'author', 'created_at']
```
- 목적: 목록 조회할 때 불필요한 데이터 제외
- 예: 글 목록 화면에서는 content, updated_at 불필요 → 데이터 전송량 감소
- Spring Boot에서는 View용 DTO를 따로 만들어 쓰는 것과 유사

| 항목                   | 역할            | Spring Boot 비교                    |
| -------------------- | ------------- | --------------------------------- |
| `PostSerializer`     | 모델 ↔ JSON 변환  | DTO + Jackson                     |
| `PostListSerializer` | 목록 조회용 경량 DTO | 목록 조회용 DTO                        |

### SpringBoot와 비교

| 기능        | Django Serializer             | Spring Boot                    |
| --------- | ----------------------------- | ------------------------------ |
| 모델 ↔ JSON | `serializers.ModelSerializer` | DTO + Jackson                  |
| 필드 선택     | `fields` 옵션                   | DTO 클래스에서 필드 정의                |
| 읽기 전용 필드  | `read_only_fields`            | DTO에서 Setter 생략하거나 @JsonIgnore |
| CRUD 처리   | ViewSet과 연계하면 자동              | Controller에서 수동 매핑 필요          | 


